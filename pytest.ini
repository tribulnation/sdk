[pytest]
asyncio_mode = auto
testpaths = packages
; Several packages have a test_lifecycle.py/test_market.py/test_paging.py of their own,
; and the test dirs are not packages. importlib import mode keys modules by path rather
; than basename, so those no longer collide at collection.
addopts = --import-mode=importlib
